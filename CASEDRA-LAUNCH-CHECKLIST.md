# Casedra Launch Checklist

This repository is already aligned to the `Casedra` brand across app copy, docs, package scope, and local project state.

## Core Setup

1. Provision `casedra.cloud` and the core mailboxes:
   `product@casedra.cloud`, `clara@casedra.cloud`, and `javier@casedra.cloud`.
2. Create the booking surface at `https://cal.com/product@casedra.cloud/demo` or replace that placeholder with the final public scheduling URL.
3. Name all new vendor-facing projects `casedra` from the start:
   Stripe Projects CLI, Vercel, Clerk, Convex, Supabase, PostHog, and GitHub.
4. Keep billing descriptors, app store metadata, social handles, and legal docs aligned to `Casedra`.

## Verification

1. Run a repo-wide search for `Casedra` and confirm the public copy reads consistently.
2. Run a repo-wide search for stale legacy names before each launch milestone.
3. Rebuild the web app after any provider or domain changes.
